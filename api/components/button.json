{
  "name": "Button",
  "route": "/button",
  "import": "import Button from '@box-kite/react/components/button';",
  "description": "A real `<button>` with the `button` component style on it — the variants are `primary` (the default), `secondary` and `ghost`, and every Box prop styles it on top of them.",
  "pattern": null,
  "props": [
    {
      "name": "props",
      "type": "ButtonTagProps",
      "required": false,
      "default": null,
      "description": "Attributes for the `<button>` itself — `disabled`, `aria-label`, `form`, `value`."
    },
    {
      "name": "type",
      "type": "ButtonType",
      "required": false,
      "default": null,
      "description": "`button` unless you say otherwise. Inside a form a `<button>` with no type submits it."
    },
    {
      "name": "onClick",
      "type": "(e: React.MouseEvent<HTMLButtonElement, MouseEvent>) => void",
      "required": false,
      "default": null,
      "description": "Fires on a click, and on Enter or Space, which the browser turns into one."
    }
  ],
  "parts": [],
  "keyboard": [
    { "mode": null, "keys": "Tab", "action": "Focuses the button; Tab again leaves it." },
    {
      "mode": null,
      "keys": "Enter / Space",
      "action": "Activates it. The browser fires a real click, so `onClick` is all a button needs to be operable from the keyboard."
    }
  ],
  "a11y": [
    "A native `<button>`, so the role, the tab stop, Enter and Space, and the disabled state are the platform's. What names it is its own text; an icon-only button needs `props={{ 'aria-label': … }}`.",
    "`disabled` in `props` removes it from the tab order, which also silences it — for a control that has to stay reachable and explain itself, `aria-disabled` with a handler that does nothing."
  ],
  "axe": { "fixtures": ["Button"], "knownViolations": [] },
  "styleTree": [{ "path": "button", "depth": 0, "extends": null, "variants": ["secondary", "ghost"] }]
}
