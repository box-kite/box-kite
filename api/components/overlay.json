{
  "name": "Overlay",
  "route": "/overlay",
  "import": "import Overlay from '@box-kite/react/components/overlay';",
  "description": "A floating layer: anchored by the browser, portalled for the stacking order. `position-area` puts it on the side of its anchor asked for and `position-try-fallbacks` flips it when there is no room, so on the CSS path nothing here runs at all — no measurement, no scroll listener, no state (`useAnchorPosition` measures instead where the browser cannot, and the layer is `position: fixed` either way).",
  "pattern": null,
  "props": [
    {
      "name": "side",
      "type": "AnchorSide",
      "required": false,
      "default": "'bottom'",
      "description": "Which side of the anchor the layer sits on: `top`/`bottom` are the block axis, `start`/`end` the inline one, so a layer beside its anchor mirrors in a right-to-left page. Default `'bottom'`."
    },
    {
      "name": "align",
      "type": "AnchorAlign",
      "required": false,
      "default": "'center'",
      "description": "Which of the anchor's edges to line up with along the other axis. Default `'center'`."
    },
    {
      "name": "offset",
      "type": "number",
      "required": false,
      "default": "0",
      "description": "The gap between anchor and layer, on the ÷4 spacing scale — `offset={2}` is 8px. Default `0`."
    },
    {
      "name": "flip",
      "type": "boolean",
      "required": false,
      "default": "true",
      "description": "Whether a side with no room may be swapped for its opposite. Default `true`. It shadows the CSS prop of that name — a mirrored layer is a rarity, and one placement vocabulary across the hook, this and `Tooltip` is worth more than the transform, which a child of the layer can still take."
    },
    {
      "name": "onSideChange",
      "type": "?(side: AnchorSide): void;",
      "required": false,
      "default": null,
      "description": "Fires with the side the layer ended up on, which is the requested one unless a flip moved it — what a popup that grows away from its trigger needs to know to animate the right way. Asking for it is what turns on the one read the CSS path otherwise never does, so leave it out when nothing uses it."
    },
    {
      "name": "anchor",
      "type": "ElementLike",
      "required": false,
      "default": null,
      "description": "Anchor to this element rather than to the spot the layer was declared in. A trigger is almost always the right answer: the placeholder is a real box with no size, so `align` and `matchWidth` have nothing to work from, and inside a flex row it becomes a flex item of its own."
    },
    {
      "name": "contentRef",
      "type": "Ref<HTMLDivElement>",
      "required": false,
      "default": null,
      "description": "The content Box, which is the one that animates. `ref` is the anchored layer, which transitions nothing on purpose — so a `<Presence>` measuring an exit has to reach past it."
    },
    {
      "name": "matchWidth",
      "type": "boolean",
      "required": false,
      "default": "true",
      "description": "Whether the layer is at least as wide as its anchor. Default true, so a dropdown popup lines up with its trigger; a tooltip sizes to its own content and turns it off."
    }
  ],
  "parts": [],
  "keyboard": [],
  "a11y": [
    "No role, no `aria-*` and no focus handling: whatever renders a layer owns the pattern, and a layer given a role it does not implement is worse than one with none.",
    "The layer is portalled out of the subtree it was declared in, so it carries the direction it was read in as a `dir` of its own — a container hanging off the body inherits nothing.",
    "It is declared where it is used but rendered into a container at the end of `<body>`, so the DOM order a screen reader reads and the tab order both follow the *portal*, not the markup: whatever a layer needs said about its place in the page has to be said with `aria-controls`, `aria-owns` or a managed focus. `Popover` is the way out of that — a top-layer element stays where it was declared."
  ],
  "axe": { "fixtures": ["Overlay"], "knownViolations": [] },
  "styleTree": []
}
