{
  "name": "ProgressRing",
  "route": "/charts",
  "import": "import { ProgressRing } from '@box-kite/react/components/chart';",
  "description": "A circular progress arc: one value from 0 to 1, drawn as the filled part of a ring over its own track.",
  "pattern": null,
  "props": [
    {
      "name": "value",
      "type": "number",
      "required": true,
      "default": null,
      "description": "How full the arc is, from 0 to 1. Rounded to half a percent — see `ChartUtils.FRACTION_STEP`."
    },
    {
      "name": "thickness",
      "type": "number",
      "required": false,
      "default": "10",
      "description": "The width of the ring in view units, out of the 100 the box is wide."
    },
    {
      "name": "trackOpacity",
      "type": "NonNullable<BoxStyleProps['strokeOpacity']>",
      "required": false,
      "default": null,
      "description": "The unfilled part of the ring, as the same colour faded — so the track follows the theme too. Default `0.2`, from the style node."
    },
    {
      "name": "preserveAspectRatio",
      "type": "string",
      "required": false,
      "default": null,
      "description": "How the drawing is fitted when the box it is given is a different shape."
    },
    {
      "name": "width",
      "type": "string | number",
      "required": false,
      "default": null,
      "description": "The `width` attribute: a number in user units, or any CSS length — `\"100%\"`, `\"12rem\"`."
    },
    {
      "name": "height",
      "type": "string | number",
      "required": false,
      "default": null,
      "description": "The `height` attribute: a number in user units, or any CSS length."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Names the drawing — `role=\"img\"` and this text. Without one the `<svg>` is `aria-hidden`."
    },
    {
      "name": "children",
      "type": "React.ReactNode",
      "required": false,
      "default": null,
      "description": "What is drawn on top of the shape: the number inside a ring, a gradient in a `<Defs>`. SVG content, not a render prop."
    }
  ],
  "parts": [],
  "keyboard": [],
  "a11y": [
    "`aria-hidden` with no `label`, and a `label` makes it `role=\"img\"`. For progress a user is *waiting* on, put the value in text or in a `role=\"progressbar\"` of your own — an image role does not announce a change."
  ],
  "axe": { "fixtures": ["ProgressRing"], "knownViolations": [] },
  "styleTree": [
    { "path": "progressRing", "depth": 0, "extends": null, "variants": [] },
    { "path": "progressRing.track", "depth": 1, "extends": null, "variants": [] },
    { "path": "progressRing.arc", "depth": 1, "extends": null, "variants": [] }
  ]
}
